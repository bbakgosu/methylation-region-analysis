name: methylation-analysis
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  # Python
  - python>=3.7

  # Data analysis
  - pandas>=1.3.0

  # Oxford Nanopore modkit
  - bioconda::ont-modkit

  # Optional: useful utilities
  - samtools  # For BAM file inspection and troubleshooting

  # Pip packages (if any additional packages are needed)
  - pip
  - pip:
    # Add any pip-only packages here if needed in the future
